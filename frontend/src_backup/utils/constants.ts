// Auth screens
export enum AuthScreens {
  LOGIN = "Login",
}

// Main dashboards for each role
export enum MainScreens {
  ADMIN_DASHBOARD = "AdminDashboard",
  VOLUNTEER_DASHBOARD = "VolunteerDashboard",
  USER_DASHBOARD = "UserDashboard",
}

// User roles
export enum UserRole {
  ADMIN = "admin",
  VOLUNTEER = "volunteer",
  USER = "user",
}
